#!/bin/sh
# Remove Cursor agent co-author so GitHub attributes commits to you only.
MSG_FILE="$1"
case "$(uname -s)" in
  Darwin) sed -i '' '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "$MSG_FILE" ;;
  *) sed -i '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "$MSG_FILE" ;;
esac
